#pragma once

#include <mutex>
#include <shared_mutex>

namespace babrat {

    using std::mutex;

    using std::shared_mutex;

    using std::unique_lock;

    using std::lock_guard;

} // namespace babrat